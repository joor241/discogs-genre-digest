# Local secrets - never commit these
.env
.env.local

# Local digest preview written by --out. Scoped to the repo root on purpose:
# a blanket *.html would also swallow the published pages under docs/.
/digest.html

# Python
__pycache__/
*.py[cod]
.venv/
venv/
